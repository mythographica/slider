# please click Root dot 2 find Mdx

<Chart id='chart' opts={{
	title: '"this" is Inheritance tree of slides',
	chart : {
		type : 'tree',
		chartData : {
			name : 'app',
			children : app.collectTypes(),
			// children : app.collectTypes(app.print ? null : 'View'),
		},
		print : app.print,
		dy : 20,
		dx : 55,
		width : 350,
		margin : {
			top: 25, right: 40, bottom: 10, left: 40
		},
		stash : ['Root']
	}
}} />

-----

# don't forget to check `./public/slides`

## 4 printing just add `?print` to the end of URL and Save it as PDF

-----

# don't forget to use `./gen.sh` after adding slides

# it re-generates `./public/slides/list.txt`

# ^ FullHD ± better

-----

# thank you

